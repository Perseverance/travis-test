import { Image404Directive } from './image404.directive';

describe('Image404Directive', () => {
  it('should create an instance', () => {
    const directive = new Image404Directive();
    expect(directive).toBeTruthy();
  });
});

import { TextLimitationPipe } from './text-limitation.pipe';

describe('TextLimitationPipe', () => {
  it('create an instance', () => {
    const pipe = new TextLimitationPipe();
    expect(pipe).toBeTruthy();
  });
});
